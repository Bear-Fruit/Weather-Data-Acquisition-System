extends layout

block content
  
  h1 Weather Now
  
  div#data
  script(type='text/javascript').
    $(document).ready(function(){
    setInterval (function(){$("#data").load('update_data')}, 1000 );
    });